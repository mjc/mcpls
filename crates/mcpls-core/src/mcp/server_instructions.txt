MCPLS-first: workspace_symbol_search then inspect_symbol; queries/targets. lexical_search; ast-grep/SSR. Reuse source frames, symbol_handle, snapshot resources; read refs before shell; stale handles; preview/apply. no registration/activation; attach/wake. Pass project_id; configured skill files directly; outside.
