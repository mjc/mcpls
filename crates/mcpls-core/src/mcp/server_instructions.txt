MCPLS-first: workspace_symbol_search then inspect_symbol; batch names/handles. lexical_search for literal/regex source search; ast-grep/SSR for structural edits. Reuse source frames, symbol_handle, snapshot resources; refresh stale handles. preview/apply edits. Pass project_id; calls attach/wake.
