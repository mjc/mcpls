MCPLS-first: workspace_symbol_search then inspect_symbol; batch. lexical_search; ast-grep/SSR. Reuse source frames, symbol_handle, snapshot resources; stale handles; preview/apply. Known projects need no registration/activation; attach/wake. Pass project_id; Read configured skill files directly; outside.
