MCPLS-first: workspace_symbol_search_batch for names, inspect_symbol_batch for handles; otherwise workspace_symbol_search then inspect_symbol. lexical_search for literal/regex text; ast-grep/SSR for structural edit. Reuse source frames, symbol_handle, snapshot resources; refresh stale handles. file read/shell only for non-source slices. preview/apply; registrations persist; sessions are not project-bound. Pass project_id; project_list once only if stable ID unknown. Calls attach/wake automatically.
