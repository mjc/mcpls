MCPLS-first: workspace_symbol_search then inspect_symbol; batch handles. lexical_search; ast-grep/SSR edits. Reuse source frames, symbol_handle, snapshot resources; refresh stale handles; preview/apply. Pass project_id; attach/wake. Read configured skill files directly; they are outside projects.
